baseURL = 'https://example.org/'
languageCode = 'en-us'
title = 'MINI PC 与 NAS 解决方案，你的家庭数据中心专家。'
theme = 'explore'
# ... [您的 title, theme, baseURL 等配置]

[menu]
  # --- 1. 迷你主机 (一级分类) [weight = 10] ---
  [[menu.main]]
    identifier = "minipc" # 【修正】使用小写无空格的 identifier
    name = "迷你主机 💻"
    url = "/minipc/"       # 【修正】使用小写无空格的 URL 路径
    weight = 10 
    
    # --- 1.1 硬件评测 (二级) ---
    [[menu.main]]
      identifier = "minipc-review"
      name = "硬件评测"
      url = "/minipc/review/"
      parent = "minipc"      # 【修正】parent 必须是 "minipc"
      weight = 11

      # --- 1.1.1 针对具体型号 (三级) ---
      [[menu.main]]
        identifier = "minipc-models"
        name = "具体型号评测"
        url = "/minipc/review/models/"
        parent = "minipc-review" 
        weight = 111

    # --- 1.2 选购指南 (二级) ---
    [[menu.main]]
      identifier = "minipc-guide"
      name = "选购指南"
      url = "/minipc/guide/"
      parent = "minipc"       # 【修正】parent 必须是 "minipc"
      weight = 12

    # --- 1.3 DIY 改造 (二级) ---
    [[menu.main]]
      identifier = "minipc-diy"  # 【修正】identifier 应使用 minipc 前缀
      name = "DIY 改造"
      url = "/minipc/diy/"      # 【修正】URL 路径应使用 minipc
      parent = "minipc"         # 【修正】parent 必须是 "minipc"
      weight = 13


  # --- 2. NAS 系统与搭建 (一级分类) [weight = 20] ---
  [[menu.main]]
    identifier = "nas"
    name = "NAS 系统与搭建 🗄️"
    url = "/nas/"
    weight = 20
    
    # --- 2.1 系统安装 (二级) ---
    [[menu.main]]
      identifier = "nas-os-install"
      name = "系统安装"
      url = "/nas/install/"
      parent = "nas"
      weight = 21

    # --- 2.2 基础配置 (二级) ---
    [[menu.main]]
      identifier = "nas-config"
      name = "基础配置"
      url = "/nas/config/"
      parent = "nas"
      weight = 22

    # --- 2.3 虚拟化/容器 (二级) ---
    [[menu.main]]
      identifier = "nas-docker"
      name = "虚拟化/容器"
      url = "/nas/docker/"
      parent = "nas"
      weight = 23


  # --- 3. 热门应用与服务 (一级分类) [weight = 30] ---
  [[menu.main]]
    identifier = "apps"
    name = "热门应用与服务 ⚙️"
    url = "/apps/"
    weight = 30

    # --- 3.1 媒体影音 (二级) ---
    [[menu.main]]
      identifier = "apps-media"
      name = "媒体影音"
      url = "/apps/media/"
      parent = "apps"
      weight = 31

    # --- 3.2 智能家居 (二级) ---
    [[menu.main]]
      identifier = "apps-home"
      name = "智能家居"
      url = "/apps/home/"
      parent = "apps"
      weight = 32

    # --- 3.3 私有云盘 (二级) ---
    [[menu.main]]
      identifier = "apps-cloud"
      name = "私有云盘"
      url = "/apps/cloud/"
      parent = "apps"
      weight = 33

    # --- 3.4 下载中心 (二级) ---
    [[menu.main]]
      identifier = "apps-dl"
      name = "下载中心"
      url = "/apps/download/"
      parent = "apps"
      weight = 34


  # --- 4. 导购与优惠 (一级分类) [weight = 40] ---
  [[menu.main]]
    identifier = "deals"
    name = "导购与优惠 💰"
    url = "/deals/"
    weight = 40

    # --- 4.1 配件清单 (二级) ---
    [[menu.main]]
      identifier = "deals-parts"
      name = "配件清单 (变现)"
      url = "/deals/parts/"
      parent = "deals"
      weight = 41

    # --- 4.2 全套配置 (二级) ---
    [[menu.main]]
      identifier = "deals-full"
      name = "全套配置 (变现)"
      url = "/deals/full/"
      parent = "deals"
      weight = 42

    # --- 4.3 优惠追踪 (二级) ---
    [[menu.main]]
      identifier = "deals-track"
      name = "优惠追踪"
      url = "/deals/tracker/"
      parent = "deals"
      weight = 43